spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: kark-profit
  jackson:
    property-naming-strategy: SNAKE_CASE
    time-zone: Asia/Phnom_Penh
  cache:
    type: caffeine

  profiles:
    active: local

  transaction:
    default-timeout: 30
    rollback-on-commit-failure: true
  datasource:
    url: ${datasource.url}
    username: ${datasource.username}
    password: ${datasource.password}
  jpa:
    hibernate:
      ddl-auto: none   # auto create/update tables
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
mailtrap:
  token: ${mailtrap.token}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  max-http-request-header-size: 1MB
  tomcat:
    max-swallow-size: 10MB
  servlet:
    context-path: /kark-profit


# Rest Client Config
#rest:
#  client:
#    # Global defaults
#    retry:
#      enabled: true
#      max-attempts: 3
#      initial-delay-millis: 1000
#      retryable-exceptions:
#        - java.net.SocketTimeoutException
#        - java.net.ConnectException
#        - Http5xx
#    connection:
#      connectTimeout: 5000
#      readTimeout: 5000
#
#    # Default client config (overrides global)
#    default-config:
#      connection:
#        connectTimeout: 10000
#        readTimeout: 10000
#
#    # Client-specific configurations
#    clients:
#      test-application:
#        connection:
#          connectTimeout: 35000
#          readTimeout: 35000
#        retry:
#          enabled: true
#          max-attempts: 5
#          initial-delay-millis: 2300
#          retryable-exceptions:
#            - java.net.SocketTimeoutException
#            - java.net.ConnectException
#            - Http5xx
#            - Http4xx
#
#      retail-teller-connector:
#        connection:
#          connectTimeout: 40000
#          readTimeout: 40000
#        retry:
#          enabled: true
#          max-attempts: 3
#          initial-delay-millis: 1000
#
#      ords-connector:
#        connection:
#          connectTimeout: 60000
#          readTimeout: 40000
#        retry:
#          enabled: false  # No retry for this API